<%INIT>
my $TicketObj = RT::Ticket->new( $session{CurrentUser} );

return unless $m->request_comp->path =~ /Ticket\/Display.html/
    && $DECODED_ARGS->{id}
    && $TicketObj->Load( $DECODED_ARGS->{id} );

PageMenu()->child(
    actions => title => loc("Request File"),
    path => ""
);
</%INIT>